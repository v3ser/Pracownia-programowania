// Zadanie 1.4.1

#include <stdio.h>
int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    for (int i = n; i < m; i += n) {
        printf("%d\n", i);
    }
    return 0;
}

// Zadanie 1.4.2
#include <stdio.h>

int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    for (int i = 1; i <= m; ++i) {
        printf("%d\n", n * i);
    }
    return 0;
}
